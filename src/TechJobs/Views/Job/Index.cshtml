@{
// TODO #2.1- declare the ViewModel (outside of this @{ } block)


}
@model TechJobs.Models.Job

<table class="job-listing">
    
    @{ 
// TODO #2.2 - display the job fields, one per row (outside of this @{ } block)
        
        
    }
    <tr>
        <th>Name</th>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <th>Employer</th>
        <td>@Model.Employer.Value</td>
    </tr>
    <tr>
        <th>Location</th>
        <td>@Model.Location.Value</td>
    </tr>
    <tr>
        <th>Skill</th>
        <td>@Model.CoreCompetency.Value</td>
    </tr>    
    <tr>
        <th>Position Type</th>
        <td>@Model.PositionType.Value</td>

    </tr>
</table>